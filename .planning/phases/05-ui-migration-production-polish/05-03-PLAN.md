---
phase: 05-ui-migration-production-polish
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - components/contact-list-section.tsx
  - components/contact-card.tsx
  - components/contact-detail-content.tsx
  - components/add-contact-modal.tsx
  - components/add-contact-button.tsx
  - components/lend-to-contact-modal.tsx
  - components/borrow-request-modal.tsx
  - components/borrow-request-card.tsx
  - components/invite-user-modal.tsx
  - components/invite-user-button.tsx
  - components/notification-panel.tsx
  - components/notification-bell.tsx
  - components/notification-item.tsx
  - app/contacts/page.tsx
  - app/contacts/[id]/page.tsx
  - app/groups/page.tsx
  - app/groups/[id]/page.tsx
  - app/groups/[id]/items/new/page.tsx
  - app/groups/join/[inviteCode]/page.tsx
  - app/borrow/page.tsx
  - app/users/[id]/page.tsx
  - app/discover/page.tsx
  - app/auth/page.tsx
  - app/about/page.tsx
autonomous: true
requirements: [UIM-01, UIM-02, UIM-03]

must_haves:
  truths:
    - "All contact, group, and borrow components use CSS variable classes with no hardcoded gray values"
    - "Notification panel and bell use CSS variable classes and look correct in dark mode"
    - "All remaining page routes use CSS variable classes"
    - "All modals that were using raw createPortal now use ui/Modal primitive for shell"
    - "Both light and dark mode display consistent colors across all migrated components"
  artifacts:
    - path: "components/contact-list-section.tsx"
      provides: "Contact list with CSS variable colors and ui primitives"
      contains: "var(--"
    - path: "components/notification-panel.tsx"
      provides: "Notification panel with CSS variable colors"
      contains: "var(--"
    - path: "app/groups/page.tsx"
      provides: "Groups page with CSS variable colors"
      contains: "var(--"
    - path: "app/auth/page.tsx"
      provides: "Auth page with CSS variable colors"
      contains: "var(--"
  key_links:
    - from: "components/contact-list-section.tsx"
      to: "components/ui/index.ts"
      via: "import primitives from barrel export"
      pattern: "import.*from.*@/components/ui"
    - from: "components/add-contact-modal.tsx"
      to: "components/ui/modal.tsx"
      via: "import Modal from ui"
      pattern: "import.*Modal.*from.*@/components/ui"
---

<objective>
Migrate all remaining components — contacts, groups, borrow, notifications, auth, and remaining pages — to CSS variable classes and design system primitives.

Purpose: After Plan 02 migrated navigation + dashboard + items, this plan completes the remaining half of the codebase. After this plan, zero hardcoded gray values should remain across the entire app.

Output: ~24 files migrated. Combined with Plan 02, all application components use CSS variables and ui/ primitives.
</objective>

<execution_context>
@/Users/connorlee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorlee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-migration-production-polish/05-RESEARCH.md
@.planning/phases/05-ui-migration-production-polish/05-CONTEXT.md
@.planning/phases/03-design-system-foundation/03-01-SUMMARY.md
@.planning/phases/03-design-system-foundation/03-02-SUMMARY.md
@.planning/phases/05-ui-migration-production-polish/05-02-SUMMARY.md
@components/ui/index.ts
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate contact and borrow components</name>
  <files>
    components/contact-list-section.tsx
    components/contact-card.tsx
    components/contact-detail-content.tsx
    components/add-contact-modal.tsx
    components/add-contact-button.tsx
    components/lend-to-contact-modal.tsx
    components/borrow-request-modal.tsx
    components/borrow-request-card.tsx
    app/contacts/page.tsx
    app/contacts/[id]/page.tsx
    app/borrow/page.tsx
  </files>
  <action>
For ALL listed files, apply the standard color migration:

**Color replacement:**
- `bg-white` → `bg-[var(--bg-base)]`
- `bg-gray-50` → `bg-[var(--bg-surface)]`
- `bg-gray-100/200` → `bg-[var(--bg-elevated)]`
- `text-gray-900/700` → `text-[var(--text-primary)]`
- `text-gray-600/500` → `text-[var(--text-secondary)]`
- `text-gray-400/300` → `text-[var(--text-tertiary)]`
- `border-gray-200/300` → `border-[var(--border)]`
- All `hover:bg-gray-*` → `hover:bg-[var(--bg-surface)]` or `hover:bg-[var(--bg-elevated)]`
- **Remove** ALL `dark:bg-gray-*`, `dark:text-gray-*`, `dark:border-gray-*` overrides on migrated elements

**Primitive migration:**
- Replace raw `<button>` with `Button` from `@/components/ui` where appropriate
- Replace raw `<input>` with `Input` from `@/components/ui` where appropriate
- For `add-contact-modal.tsx`, `lend-to-contact-modal.tsx`, `borrow-request-modal.tsx`: migrate to use `Modal`, `ModalHeader`, `ModalBody`, `ModalFooter` from `@/components/ui` for the modal shell, keeping internal form logic intact
- Ensure `<textarea>` and `<select>` elements get CSS variable classes directly

**Specific attention:**
- `contact-list-section.tsx` has a search input — migrate to `Input` from ui
- `contact-detail-content.tsx` has borrow history display — use CSS variable classes for the status displays
- `borrow-request-modal.tsx` handles accept/reject — use Button variants (primary for accept, destructive for reject)

Use the frontend-design skill to ensure migrated components look polished.
  </action>
  <verify>
Run `npm run build` — no type errors. Grep all 11 files for `bg-white`, `text-gray-`, `border-gray-` — should find zero matches. Grep for `dark:bg-gray-`, `dark:text-gray-`, `dark:border-gray-` — zero matches.
  </verify>
  <done>
All contact and borrow components fully migrated. Modals use ui/Modal shell. Buttons use ui/Button. Inputs use ui/Input. Zero hardcoded gray values remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate notification, group, and remaining page components</name>
  <files>
    components/notification-panel.tsx
    components/notification-bell.tsx
    components/notification-item.tsx
    components/invite-user-modal.tsx
    components/invite-user-button.tsx
    app/groups/page.tsx
    app/groups/[id]/page.tsx
    app/groups/[id]/items/new/page.tsx
    app/groups/join/[inviteCode]/page.tsx
    app/users/[id]/page.tsx
    app/discover/page.tsx
    app/auth/page.tsx
    app/about/page.tsx
  </files>
  <action>
For ALL listed files, apply the standard color migration:

**Color replacement (same mapping as Task 1):**
- `bg-white` → `bg-[var(--bg-base)]`
- `bg-gray-50` → `bg-[var(--bg-surface)]`
- `bg-gray-100/200` → `bg-[var(--bg-elevated)]`
- `text-gray-900/700` → `text-[var(--text-primary)]`
- `text-gray-600/500` → `text-[var(--text-secondary)]`
- `text-gray-400/300` → `text-[var(--text-tertiary)]`
- `border-gray-200/300` → `border-[var(--border)]`
- All `hover:bg-gray-*` → `hover:bg-[var(--bg-surface)]` or `hover:bg-[var(--bg-elevated)]`
- **Remove** ALL `dark:bg-gray-*`, `dark:text-gray-*`, `dark:border-gray-*` overrides on migrated elements

**Primitive migration:**
- Replace raw `<button>` with `Button` from `@/components/ui` where appropriate
- Replace raw `<input>` with `Input` from `@/components/ui` where appropriate
- `invite-user-modal.tsx` — migrate to use `Modal` from ui
- `notification-panel.tsx` — migrate dropdown/panel styling to CSS variable classes. This is a slide-out panel, not a modal, so do NOT wrap in Modal primitive — just migrate the colors.
- `notification-item.tsx` — use Badge from ui for notification status if applicable

**For page routes:**
- `app/groups/page.tsx` — color migration + use Button from ui for "Create Group" CTA
- `app/groups/[id]/page.tsx` — color migration + server component, so only CSS variable class changes (no client component imports)
- `app/groups/[id]/items/new/page.tsx` — color migration for the new item form
- `app/groups/join/[inviteCode]/page.tsx` — color migration
- `app/users/[id]/page.tsx` — color migration for profile display
- `app/discover/page.tsx` — color migration
- `app/auth/page.tsx` — color migration for login/signup form. Use Input from ui if applicable.
- `app/about/page.tsx` — color migration for static content

Use the frontend-design skill for polished results.
  </action>
  <verify>
Run `npm run build` — no type errors. Grep ALL 13 files for `bg-white`, `text-gray-`, `border-gray-` — zero matches. Grep for `dark:bg-gray-`, `dark:text-gray-`, `dark:border-gray-` — zero matches.
  </verify>
  <done>
All notification, group, and remaining page components fully migrated. Zero hardcoded gray values in any application component. The entire codebase uses CSS variable classes for neutral colors.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. Zero instances of hardcoded `bg-white` in any application component (grep across all components/ and app/ .tsx files)
3. Zero instances of `text-gray-` (non-dark:) in any application component
4. Zero instances of `border-gray-` (non-dark:) in any application component
5. Zero instances of `dark:bg-gray-`, `dark:text-gray-`, `dark:border-gray-` in any application component (except Badge/primitive palette-specific dark: variants which are correct by design)
6. Modals use ui/Modal for their shell
</verification>

<success_criteria>
- Every application component in the codebase uses CSS variable classes for neutral colors
- All modals use ui/Modal primitive for their shell
- Contact, group, and borrow flows work correctly in both light and dark mode
- Auth page renders correctly in both modes
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-migration-production-polish/05-03-SUMMARY.md`
</output>
